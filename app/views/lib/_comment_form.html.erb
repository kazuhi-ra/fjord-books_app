<%= form_with model: [commentable, commentable.comments.new], local: true do |form| %>
  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content, required: true %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
